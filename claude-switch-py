#!/usr/bin/env sh
# ============================================================
# claude-switch-py — Profile switcher (Python version) / 配置文件切换器（Python 版）
# Requires Python 3.6+ / 需要 Python 3.6 及以上版本
# Usage / 用法: claude-switch-py [list|ls|current|cur|<profile-name>]
# ============================================================
DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHONIOENCODING=utf-8 python3 "$DIR/claude-switch.py" "$@" 2>/dev/null || \
PYTHONIOENCODING=utf-8 python  "$DIR/claude-switch.py" "$@"
